6.1 环境设计思路
我们将第一阶段实现的单轴卫星仿真封装成符合Gym接口的环境。环境需要定义：

__init__：定义状态空间、动作空间、物理参数

reset()：重置环境到随机初始状态

step(action)：执行一步仿真，返回新状态、奖励、是否结束、信息

render()：可选，用于可视化

6.2 实现 SatelliteEnv 类
创建一个新文件 satellite_env.py：

6.3 测试环境
在同一目录下创建测试脚本 test_env.py：


你的任务：

运行测试，观察随机策略下的表现（应该很差）。

尝试修改奖励函数的权重，观察对智能体行为的影响（目前只是随机策略，不影响训练，但为后续打基础）。